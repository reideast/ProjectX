{
    "name": "proj",
    "private": true,
    "scripts": {
        "start": "meteor run"
    },
    "dependencies": {
        "bcrypt": "^1.0.2",
        "dropbox": "^0.10.3",
        "meteor-node-stubs": "~0.2.0",
        "autoprefixer": "^6.4.1"
    },
    "postcss": {
        "plugins": {
            "autoprefixer": {
                "browsers": [ "last 2 versions" ]
            }
        }
    }
}
